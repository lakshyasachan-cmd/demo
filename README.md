# demo
task- Create a public GitHub repository for a single page React application for a To-Do List. 
The app should allow users to input strings, track them in a list, display the list, and check off items.

used technologies -HTML, CSS, JS 
